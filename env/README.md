# Environment

This directory defines the interface between the simulation and the agent logic.

It is intended to host observation extraction, action formatting, reward definitions, and the environment step/reset workflow.
